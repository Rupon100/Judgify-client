import axios from 'axios';

const Services = () => {
    // const { data } = await axios.get(`${import.meta.env.VITE_API_URL}/add-service`, dataInfo);

    // const fetchAll = async () => {
    //     const { data } = await axios.get(
    //       `${import.meta.env.VITE_API_URL}/services}`
    //     );
    // console.log(data);

    return (
        <div>
            Services here
        </div>
    );
};

export default Services;